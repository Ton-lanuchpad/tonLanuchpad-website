import Wrapper from '../layout/Wrapper';

import EndToEndMain from '../components/end-to-end';

const EndToEnd = () => {
   return (
      <Wrapper>
        
         <EndToEndMain />
      </Wrapper>
   );
};

export default EndToEnd;